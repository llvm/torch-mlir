set(LLVM_TARGET_DEFINITIONS Passes.td)
if(TORCH_MLIR_ENABLE_STABLEHLO)
  if(TORCH_MLIR_ENABLE_TCP)
    mlir_tablegen(Passes.h.inc -gen-pass-decls -DTORCH_MLIR_ENABLE_STABLEHLO -DTORCH_MLIR_ENABLE_TCP)
  else()
    mlir_tablegen(Passes.h.inc -gen-pass-decls -DTORCH_MLIR_ENABLE_STABLEHLO)
  endif()
else()
  if(TORCH_MLIR_ENABLE_TCP)
    mlir_tablegen(Passes.h.inc -gen-pass-decls -DTORCH_MLIR_ENABLE_TCP)
  else()
    mlir_tablegen(Passes.h.inc -gen-pass-decls)
  endif()
endif()
add_public_tablegen_target(TorchMLIRTorchConversionPassIncGen)

add_mlir_doc(Passes TorchMLIRTorchConversionTransforms ./ -gen-pass-doc)
